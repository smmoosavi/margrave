/**
 * @public
 */
export interface Options {}

/**
 *
 * return `margrave` string
 *
 * @returns the `margrave`  string
 *
 * @example
 * Here's an example:
 *
 * ```ts
 * import { Margrave } from 'margrave'
 *
 * console.log(Margrave());
 * // Prints "margrave":
 * ```
 */
export function Margrave(options?: Options) {
  return 'margrave';
}
